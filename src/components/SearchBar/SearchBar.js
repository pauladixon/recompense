import React from 'react';

const SearchBar = (props) => {

  return (
    <div className="search-bar">
        search by location
        <br></br>
        search by category
    </div>

  );
};

export default SearchBar;